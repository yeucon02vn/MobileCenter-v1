const BOLD = "!font-bold "
const SANS = "!font-sans"
export const typography = {
  h1: `${BOLD} ${SANS} !text-6xl`, // 64
  h2: `${BOLD} ${SANS} !text-5xl`, // 48
  h3: `${BOLD} ${SANS} !text-4xl`, // 36
  h4: `${BOLD} ${SANS} !text-3xl`, // 30
  h5: `${BOLD} ${SANS} !text-2xl`, // 24
  h6: ` !text-xl`, // 20
}
